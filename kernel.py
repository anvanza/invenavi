#invenvi core file

import logging
import os

class invenaviKernel:
  def __init__(self, config, debug=False):
        self.config = config
        self.debug = debug
  def update(self):
    pass
  def halt(self):
    pass
